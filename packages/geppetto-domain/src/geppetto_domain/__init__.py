"""geppetto-domain: saf domain çekirdeği.

Kural (ADR-0001): IO yok, HTTP yok, SQL yok, tenant yok, saat yok, ortam yok.
Yalnızca pydantic'e bağımlıdır. Uzunluklar tam sayı µm, alan adları `_um` soneklidir (ADR-0002).
"""

__version__ = "0.1.0"
